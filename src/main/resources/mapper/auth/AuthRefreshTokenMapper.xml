<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prj.codingstudy.core.mapper.AuthRefreshTokenMapper">
    <insert id="insertRefreshToken" parameterType="AuthRefreshToken">
        insert into auth_refresh_token (MBR_ID,
                                        MBR_REFRESH_TOKEN,
                                        EXPIRED_YN,
                                        EXPIRED_DT,
                                        DEL_YN,
                                        REG_DT,
                                        MOD_DT)
        values (
                #{mbrId}
                , #{mbrRefreshToken}
                , #{expiredYn}
                , #{expiredDt}
                , #{delYn}
                , now()
                , now()
               )
    </insert>

    <update id="updateRefreshToken" parameterType="AuthRefreshToken">
        update auth_refresh_token
        set  MBR_REFRESH_TOKEN = #{mbrRefreshToken}
        , EXPIRED_YN = #{expiredYn}
        , EXPIRED_DT = #{expiredDt}
        , MOD_DT = now()
        where MBR_ID = #{MBR_ID}
    </update>

    <delete id="deleteRefreshToken" parameterType="AuthRefreshToken">
        update auth_refresh_token
        set  MBR_REFRESH_TOKEN = #{mbrRefreshToken}
          , DEL_YN = #{delYn}
          , MOD_DT = now()
        where MBR_ID = #{MBR_ID}
    </delete>
</mapper>